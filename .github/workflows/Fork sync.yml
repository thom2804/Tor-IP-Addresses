name: Fork sync
run-name: ${{ github.actor }}
on: [push]
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - name: Fork Sync2
      uses: zwping/fork-sync@v1.6.5
      with:
        owner: thom2804
        token: ${{github.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}}
        base: Tor-IP-Addresses
        head: Tor-IP-Addresses
